# country_explorer
User types the name of a country, clicks “Search,” and the page fetches info about that country from the REST Countries API and displays it (flag, capital, population, languages, etc.).
